# NodeX Vocal — dependencies
# Training (install locally, not on server):
torch>=2.0.0
torchaudio>=2.0.0
musdb>=0.4.0

# Inference (install on server):
onnxruntime>=1.17.0
numpy>=1.26.0
librosa>=0.10.0
soundfile>=0.12.0
